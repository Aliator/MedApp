<div class="card-section">
    @if (!string.IsNullOrEmpty(Title))
    {
        <h2 class="section-title">@Title</h2>
    }
    @if (UseGrid)
    {
        <div class="card-grid">
            @ChildContent
        </div>
    }
    else
    {
        @ChildContent
    }
</div>