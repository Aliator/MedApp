@inherits LayoutComponentBase
@inject NavigationManager Nav

@if (Nav.Uri.EndsWith("/login"))
{
    @Body
}
else
{
    <div class="page">
        <div class="sidebar">
            <NavMenu />
        </div>

        <main>
            @Body
        </main>
    </div>
}